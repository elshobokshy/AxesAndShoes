{% extends 'layout.twig' %}

{% block body %}

<<<<<<< d1f11094b78696c539f7a4bdbef218bf14c8359a
<<<<<<< 38375fb6be378e7e2b4f4b89b1e6c4ad29941701
    <form action="{{ path_for('search') }}" method="POST">
        <input type="text" class="form-control" placeholder="Search" name="search">
        {{ csrf() }}
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

    <div class="row">
        {% for p in products %}
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="{{ asset( p.img ) }}" alt="...">
                    <div class="caption">
                        <h3>{{ p.title }}</h3>
                        <p>{{ p.price }}€</p>
                        <p>
                            <a href="/detail/{{ p.id }}" class="btn btn-success" role="button">See product's details</a>
                        </p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
=======
    <form action="/search">
=======
    <form action="{{ path_for('search') }}" method="POST">
>>>>>>> The real price is now displayed. The "product" page's been renamed to "detail" page. The template has also been applied to it.
        <input type="text" class="form-control" placeholder="Search" name="search">
        {{ csrf() }}
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

<<<<<<< d1f11094b78696c539f7a4bdbef218bf14c8359a
>>>>>>> commit before rebase
=======
    <div class="row">
        {% for p in products %}
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="{{ asset( p.img ) }}" alt="...">
                    <div class="caption">
                        <h3>{{ p.title }}</h3>
                        <p>{{ p.price }}€</p>
                        <p>
                            <a href="/detail/{{ p.id }}" class="btn btn-success" role="button">See product's details</a>
                        </p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
>>>>>>> The real price is now displayed. The "product" page's been renamed to "detail" page. The template has also been applied to it.
{% endblock %}